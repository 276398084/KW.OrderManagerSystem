
@model IEnumerable<KeWeiOMS.Domain.UserType>
@{
    ViewBag.Title = "Index";
}
<body class="easyui-layout">
<script language="javascript" type="text/javascript">
    $(function () {
        $('#dg').datagrid({
            url: '@Url.Action("List","User")',
            pageNumber: 1,
            pageSize: 20,
            pageList: [20, 40, 60, 80, 100]
        });

    });

    //新建
    function c_dlg() {
        var url_str = '@Url.Action("Create","User")';
		
        $('#c_dlg').load(url_str, function () {
            $(this).dialog({
                title: '新建',
				modal:true,
				loadingMessage:'正在加载...',
                buttons: [{
                    text: '提交',
                    iconCls: 'icon-ok',
                    handler: function () {
                        ajaxAdd();
                    }
                }, {
                    text: '取消',
                    handler: function () {
                        $('#c_dlg').dialog('close');
                    }
                }]
            });
        }).show();
    }

    //编辑框
    function e_dlg() {
        var id = getselectedRow();
        if (id != undefined) {
            var url = '@Url.Action("Edit","User")/' + id;
            $('#e_dlg').load(url, function () {
                $(this).dialog({
                    title: '编辑',
					modal:true,
					loadingMessage:'正在加载...',
                    buttons: [{
                        text: '提交',
                        iconCls: 'icon-ok',
                        handler: function () {
							ajaxEdit();
                        }
                    }, {
                        text: '取消',
                        handler: function () {
                            $('#e_dlg').dialog('close');
                        }
                    }]
                });
            }).show();
        }
    }

//异步新建提交
        function ajaxAdd() {
            $('#c_form').form('submit', {
                url: '@Url.Action("Create","User")',
                onSubmit: function () {
                    return $(this).form('validate');
                },
                success: function (result) {
                    var result = eval('(' + result + ')');
                    if (result.errorMsg) {
                       $.messager.show({
							title: '提示',
							msg: '保存失败:' + result.errorMsg,
							timeout: 0,
							showType: 'slide'
						});
                    } else {
                        $('#c_dlg').dialog('close');
                        $('#dg').datagrid('reload');
                        $.messager.show({
                            title: '提示',
                            msg: '保存成功',
                            timeout: 2000,
                            showType: 'slide'
                        });
                    }
                }
            });
        }
   //异步修改提交
        function ajaxEdit() {
            $('#e_form').form('submit', {
                url: '@Url.Action("Edit","User")',
                onSubmit: function () {
                    return $(this).form('validate');
                },
                success: function (result) {
                    var result = eval('(' + result + ')');
                    if (result.errorMsg) {
                       $.messager.show({
							title: '提示',
							msg: '保存失败:' + result.errorMsg,
							timeout: 0,
							showType: 'slide'
						});
                    } else {
                        $('#e_dlg').dialog('close');
                        $('#dg').datagrid('reload');
                        $.messager.show({
                            title: '提示',
                            msg: '保存成功',
                            timeout: 2000,
                            showType: 'slide'
                        });
                    }
                }
            });
        }
   

    //删除
    function del() {
        var id = getselectedRow();
        if (id != undefined) {
            $.messager.confirm('确认', '确定删除?', function (r) {
                if (r) {
                    var url = '@Url.Action("Delete","User")/' + id;
                    $.post(url, function () {
                    }).success(function (data) {
                        var msgstr = "删除成功";
                        if (data != true) {
                            msgstr = "删除失败" + data;
							$.messager.show({
	                            title: '提示',
	                            msg: msgstr,
								 timeout: 0,
	                            showType: 'slide'
                        	});
                        }else{
	                        $.messager.show({
	                            title: '提示',
	                            msg: msgstr,
	                            timeout: 3000,
	                            showType: 'slide'
	                        });
						}

                        $('#dg').datagrid('reload');
                    }) .error(function () {
                            $.messager.alert('错误', '删除发生错误');
                        });

                }
            });
        }
    }

    //获取选中行
    function getselectedRow() {
        var row = $('#dg').datagrid('getSelected');
        if (row != undefined) {
            if (row.hasOwnProperty('Id')) {
                var id = row['Id'];
                return id;
            }
        }
        $.messager.show({
            title: '提示',
            msg: '请选择行',
            timeout: 3000,
            showType: 'slide'
        });
        return (undefined);
    }
   
</script>
<div region="center">
<table id="dg" class="easyui-datagrid" 
           toolbar="#toolbar"  
           rownumbers="true" fitColumns="true" singleSelect="true" pagination="true" fit="true">  
        <thead>  
        <tr>
            <th field="Id" width="100">
                主键
            </th>

            <th field="Code" width="100">
                编号
            </th>

            <th field="Username" width="100">
                登录名
            </th>

            <th field="Password" width="100">
                用户密码
            </th>

            <th field="Realname" width="100">
                姓名
            </th>

            <th field="RoleId" width="100">
                默认角色主键
            </th>

            <th field="SecurityLevel" width="100">
                安全级别
            </th>

            <th field="CId" width="100">
                公司代码
            </th>

            <th field="CompanyName" width="100">
                公司
            </th>

            <th field="DId" width="100">
                部门主键
            </th>

            <th field="DepartmentName" width="100">
                部门名称
            </th>

            <th field="WorkgroupId" width="100">
                工作组主键
            </th>

            <th field="WorkgroupName" width="100">
                工作组名称
            </th>

            <th field="Gender" width="100">
                性别
            </th>

            <th field="Telephone" width="100">
                电话
            </th>

            <th field="Mobile" width="100">
                手机
            </th>

            <th field="Birthday" width="100">
                出生日期
            </th>

            <th field="Duty" width="100">
                岗位
            </th>

            <th field="ChangePasswordDate" width="100">
                最后修改密码日期
            </th>

            <th field="OICQ" width="100">
                QQ号码
            </th>

            <th field="Email" width="100">
                电子邮件
            </th>

            <th field="HomeAddress" width="100">
                家庭住址
            </th>

            <th field="AllowStartTime" width="100">
                允许登录时间开始
            </th>

            <th field="AllowEndTime" width="100">
                允许登录时间结束
            </th>

            <th field="LockStartDate" width="100">
                暂停用户开始日期
            </th>

            <th field="LockEndDate" width="100">
                暂停用户结束日期
            </th>

            <th field="FirstVisit" width="100">
                第一次访问时间
            </th>

            <th field="PreviousVisit" width="100">
                上一次访问时间
            </th>

            <th field="LastVisit" width="100">
                最后访问时间
            </th>

            <th field="LogOnCount" width="100">
                登录次数
            </th>

            <th field="AuditStatus" width="100">
                审核状态
            </th>

            <th field="IsVisible" width="100">
                是否显示
            </th>

            <th field="UserOnLine" width="100">
                在线状态
            </th>

            <th field="IPAddress" width="100">
                IP地址
            </th>

            <th field="MACAddress" width="100">
                MAC地址
            </th>

            <th field="OpenId" width="100">
                单点登录标识
            </th>

            <th field="DeletionStateCode" width="100">
                删除标志
            </th>

            <th field="Enabled" width="100">
                有效
            </th>

            <th field="SortCode" width="100">
                排序码
            </th>

            <th field="Description" width="100">
                备注
            </th>

            <th field="CreateOn" width="100">
                创建日期
            </th>

            <th field="CreateUserId" width="100">
                创建用户主键
            </th>

            <th field="CreateBy" width="100">
                创建用户
            </th>

            <th field="ModifiedOn" width="100">
                修改日期
            </th>

            <th field="ModifiedUserId" width="100">
                修改用户主键
            </th>

            <th field="ModifiedBy" width="100">
                修改用户
            </th>


    	</tr>
		</thead>
		</table>
        <div id="toolbar">
        <a href="#" class="easyui-linkbutton" iconCls="icon-add" plain="true" onclick="c_dlg();">新建</a>  
        <a href="#" class="easyui-linkbutton" iconCls="icon-edit" plain="true" onclick="e_dlg()">编辑</a>  
        <a href="#" class="easyui-linkbutton" iconCls="icon-remove" plain="true" onclick="del();">删除</a>  
        </div> 
        
    <div id="c_dlg"></div>
    <div id="e_dlg"></div>

</div>
</body>
