<script>

    $(function () {
        var dt = new Date().getFullYear() + '/' + (new Date().getMonth() + 1) + '/' + new Date().getDate();
        $('#dg').datagrid({
            url: '@Url.Action("OrderCount", "Statistics")',
            queryParams: { "st": dt, "et": dt, "p": "ALL", "a": "ALL" },
            showFooter: true,
            height: 400,
            width: 400,
            striped: true
        });

        $('#dg2').datagrid({
            url: '@Url.Action("OrderCountryData", "Statistics")',
            queryParams: { "st": dt, "et": dt, "p": "ALL", "a": "ALL" },
            height: 400,
            width: 400,
            showFooter: true,
            striped: true
        });
        $("#cc").datebox({
            parser: myparser
        });
        $("#cc2").datebox({
            parser: myparser
        });
        $('#pp').combobox({
            url: '/Account/Platform/1',
            valueField: 'id',
            textField: 'text',
            width: 100,
            panelHeight: 'auto',
            onChange: changePlatform
        });
        $('#pp').combobox("setValue", "ALL");

    });
    function onSelect1() {

        $('#dg').datagrid("load", {
            st: $('#cc').combobox("getValue"),
            et: $('#cc2').combobox("getValue"),
            p: $('#pp').combobox("getText"),
            a: $('#aa').combobox("getText")
        });
        $('#dg2').datagrid("load", {
            st: $('#cc').combobox("getValue"),
            et: $('#cc2').combobox("getValue"),
            p: $('#pp').combobox("getText"),
            a: $('#aa').combobox("getText")
        });
    }
    function changePlatform(n, o) {

        $('#aa').combotree({
            url: '/Account/AccountList/' + n,
            valueField: 'id',
            textField: 'text',
            width: 150

        });
        $('#aa').combotree("setValue", "ALL");

    }
    function myparser(s) {
        if (!s) return new Date();
        var ss = s.split('-');
        var y = parseInt(ss[0], 10);
        var m = parseInt(ss[1], 10);
        var d = parseInt(ss[2], 10);
        if (!isNaN(y) && !isNaN(m) && !isNaN(d)) {
            return new Date(y, m - 1, d);
        } else {
            return new Date();
        }
    }
</script>
开始<input id="cc" type="text">
结束<input id="cc2" type="text">
平台<input id="pp" type="text">
账户<input id="aa" type="text">
<a href="#" class="easyui-linkbutton" data-options="iconCls:'icon-search'" onclick="onSelect1();">查询</a>
<table>
    <tr>
        <td>
            <table id="dg" class="easyui-datagrid" title="订单数量统计"
                rownumbers="true" singleselect="true">
                <thead>
                    <tr>
                        <th field="Platform" width="80">账户
                        </th>
                        <th field="Account" width="190">账户
                        </th>
                        <th field="OCount" width="100">数量
                        </th>
                    </tr>
                </thead>
            </table>
        </td>
        <td>
            <table id="dg2" class="easyui-datagrid" title="订单国家分布"
                rownumbers="true" fitcolumns="true" singleselect="true">
                <thead>
                    <tr>
                        <th field="Key" width="190">国家
                        </th>
                        <th field="Proportion" width="100">比例
                        </th>
                        <th field="Count" width="100">订单数
                        </th>

                    </tr>
                </thead>
            </table>
        </td>
    </tr>
</table>



<table id="dg3" class="easyui-datagrid" title="订单金额区间分布"
    rownumbers="true" singleselect="true">
    <thead>
        <tr>
            <th field="Platform" width="100">区间
            </th>
            <th field="Account" width="100">订单数
            </th>
            <th field="OCount" width="100">比例
            </th>
        </tr>
    </thead>
</table>

<table id="dg3" class="easyui-datagrid" title="历史销售记录15天"
    rownumbers="true" fitcolumns="true" singleselect="true">
    <thead>
        <tr>
            <th field="Platform" width="100">区间
            </th>
            <th field="Account" width="100">订单数
            </th>
            <th field="OCount" width="100">比例
            </th>
        </tr>
    </thead>
</table>
